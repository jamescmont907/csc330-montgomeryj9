# Reflection on Resume App

Working on this project and having to put together all the files myself with no direct instructions and writing the app.js file gave me a lot better understanding of how it all works.

# Partials

I also learned a lot about partials and how using an nav, header and footer can help to reuse code and not have to copy and paste the same nav links or other elements that might go in the partials. It also helps to make sure your css is consistent accross all your pages with headers and footers

# Bugs

I had a lot of bugs involving using the ejs syntax with <%= %> to use variables and I had some trouble getting the file paths right and how it connects with the parts of the app.js file they connect with, like in Line 13 of home.ejs but now I understand it better and see its better to use the variables rather than hard code the values in the ejs file and I understand it happen because I was missing the root of the data in my app.js file
