# Resume App

My resume node.js assigment for CSC330

1. Navigate to the project: `cd assignments\resume-app`
2. Install dependencies: `npm install` and `npm install express ejs` and ` npm install dotenv`
3. Start the server: `npm start`
4. Open your browser to http://localhost:3001

# Features!
